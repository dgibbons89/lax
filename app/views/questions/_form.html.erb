<%= form_for(@question, :html => { multipart: true }) do |f| %>
  <% if @question.errors.any? %>
    <div id="error-explanation" class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@question.errors.count, "error") %> prohibited this restaurant from being saved:</h4>

      <ul>
      <% @question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  	
  <div class="form-group">
    <%= f.label :subject %><br>
    <%= f.text_field :subject, class: "form-control", placeholder: "Math" %>
  </div>
    <div class="form-group">
    <%= f.label :post %><br>
    <%= f.text_area :post, class: "form-control", placeholder: "What's your question?" %>
  </div>




  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>